<%= stylesheet_link_tag 'custom' %>

<style>
    .center {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }
</style>

<p style="color: green; text-shadow: 2px 2px 4px white;"><%= notice %></p>

<h1>Consultas</h1>

<div id="consulta" class="center">
  <% @consulta.each do |consultum| %>
    <%= render consultum %>
    <p>
      <button onclick="window.location.href='<%= consultum_path(consultum) %>'">Mostrar essa consulta</button>
    </p>
  <% end %>
</div>

<br><br>
<button onclick="window.location.href='<%= new_consultum_path %>'">Nova consulta</button> |
<button onclick="window.location.href='/'">Voltar para a página inicial</button>