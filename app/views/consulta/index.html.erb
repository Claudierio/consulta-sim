<!DOCTYPE html>
<html>
<head>
  <title>Página de Consulta</title>
  <%= stylesheet_link_tag 'custom' %>
</head>
<body>
<div class="container">
  <p style="color: green;"><%= notice %></p>

  <h1>Consultas</h1>

  <div id="consulta">
    <% @consulta.each do |consultum| %>
      <%= render consultum %>
      <p>
        <button onclick="window.location.href='<%= consultum_path(consultum) %>'">Show this consultum</button>
      </p>
    <% end %>
  </div>

  <button onclick="window.location.href='<%= new_consultum_path %>'">Nova consulta</button>
  <br> <br>
  <button onclick="window.location.href='/'">Voltar para a página inicial</button>
</div>
</body>
</html>
