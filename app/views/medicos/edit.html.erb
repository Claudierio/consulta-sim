<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        text-align: center;
        background-color: #007bff; /* Fundo azul */
    }

    h1 {
        color: #fff; /* Cor das letras branca */
        font-size: 32px;
    }

    h2 {
        color: #fff; /* Cor das letras branca */
        font-size: 24px;
        margin-top: 40px;
    }

    button {
        background-color: #fff; /* Botões brancos */
        border: none;
        color: #007bff; /* Cor das letras azul */
        padding: .9rem 4rem;
        border-radius: 3rem;
        display: inline-block;
        box-shadow: 0 0 7px 2px rgba(0, 0, 0, 0.2);
        transition: background-color 0.3s ease;
        font-weight: bold; /* Torna a letra do botão mais encorpada */
        font-family: "Helvetica Neue", Arial, sans-serif; /* Exemplo de fonte bonita */
    }

    button:hover {
        background-color: #BBBBBB;
    }
</style>

<h1>Editing medico</h1>

<%= render "form", medico: @medico %>

<br>

<div>
  <%= link_to "Show this medico", @medico %> |
  <%= link_to "Back to medicos", medicos_path %>
</div>
